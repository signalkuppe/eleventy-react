const defaultPagination = {
    nextPageLink: '#next',
    previousPageLink: '#prev',
};

const lastPage = {
    previousPageLink: '#prev',
};

const firstPage = {
    nextPageLink: '#next',
};

export { defaultPagination, lastPage, firstPage };
