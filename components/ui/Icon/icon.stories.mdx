import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Icon from './';
import Span from '../../primitives/Span';
import HomeIcon from '../../../src/icons/home.svg';

<Meta title="UI/Icon" component={Icon} />

# Icon

Svg icons

<Canvas>
    <Story name="Default">
        <Icon icon={HomeIcon} />
    </Story>
    <Story name="Left">
        <Icon icon={HomeIcon} left />
        <Span>text on the right</Span>
    </Story>
    <Story name="Right">
        <Span>text on the left</Span>
        <Icon icon={HomeIcon} right />
    </Story>
</Canvas>
